# ⚔️ STELAKOSMAK'S RIFT OF -20 ⚔️

> THE OFFICIAL UNOFFICIAL OPAP SHRINE OF MALPHITE FLASH R

A single-file, self-contained **League of Legends brainrot / trolling** website. Maximum sensory
overload: scroll depth is the master intensity knob — the deeper you go, the louder and more
layered the audio gets, until the **DEMACIA bass drop** at ~75%. Garen spins, Malphite Flash-R's,
Malzahar suppresses you, every OPAP odd resolves to **-20**, and every "helpful" button trolls you.

## Run it

It's one file — no build step.

```bash
# easiest (so the local images + audio load cleanly):
python3 -m http.server 8000
# then open http://localhost:8000
```

…or just open `index.html` in a browser. Click **ENABLE AUDIO — ENTER THE RIFT** (browsers block
autoplay until you click). **Volume warning: it's loud.** Press **Esc** / the 🛑 PANIC button to nuke
the chaos, **🔇** to mute.

## Assets (all swappable)

At the top of the `<script>` in `index.html` there's an **ASSET REGISTRY** block:

| Slot | File | What it drives |
|---|---|---|
| `GIF_GAREN_SPIN`  | `garen-spin.gif`     | spinning-Garen cursor, roamer, swarm, captcha |
| `GIF_MALPH_FLASH` | `malphite-flash.gif` | Flash-R ambush jumpscare |
| `GIF_MALZ_FLASH`  | `malzahar.gif`       | Nether Grasp SUPPRESS overlay |
| `AUD_GAREN`       | `garen-voice.mp3`    | "DEMACIA!" voice line |
| `AUD_MALPHITE`    | `malphite-voice.mp3` | "Rock solid" voice line |

The local photos in this folder are sprayed **extremely randomly** across the page (swarm / popups /
rain / captcha) — random subsets, never all at once. Swap any asset by dropping a new file in and
updating its filename in the registry.

## Features

ENTER gate · scroll-driven Web-Audio engine (5 layered drones, lowpass sweep, sub-bass, OPAP
arpeggio + kicks, DEMACIA bass drop) · 99% loading troll · live OPAP odds board (everything → -20) ·
champion shrine · testimonials & PATCH 14.ATYXOS notes · Garen-spin captcha · rigged betting slip ·
runaway buttons · click-anywhere popups + the cursed ✕ · Malphite ambush · Malzahar suppress ·
fake right-click menu · tab-title heckling · custom cursor · Esc/PANIC + mute rails.

---

© STELAKOSMAK ENTERPRISES — Not affiliated with OPAP, Riot Games, GUNGAFOODS, the KRATER, or any
winning bet ever placed. Play responsibly (you won't). Current balance: **-20**.
